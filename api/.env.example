# Example environment configuration for Samantics API
# Copy this file to .env and update with your actual values

# Embedding API configuration
# Replace with your own embedding service URL
CLOUDFLARE_URL="https://your-embedding-service-url"

# Only needed if your embedding service requires authentication
CLOUDFLARE_ID="your-access-id"
CLOUDFLARE_SECRET="your-access-secret"

# Development mode flag
# Set to "true" to skip auto-seeding embeddings (faster startup for development)
DEV="true"